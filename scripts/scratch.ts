// import { Data } from '../src';
import { Specimens, Sequence, IntegerRange, integer } from '../src';

Specimens.item([]).print(100, 10);

const s = Sequence.from([1]);

console.log(s.isEmpty());
console.log(s.isEmpty());
